import 'package:flutter/material.dart';

class AllTasksWidget extends StatelessWidget {
  const AllTasksWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigo,
      child: Text('AllTasksWidget'),
    );
  }
}
