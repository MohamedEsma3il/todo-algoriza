import 'package:flutter/material.dart';

class CompleteTasksWidget extends StatelessWidget {
  const CompleteTasksWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.teal,
      child: Text('CompleteTasksWidget'),
    );
  }
}
